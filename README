The portfolio_edit folder contains the module files. To install it, copy and paste the folder to the sites/all/modules folder.  
The template files folder contains templates. Do not install unless you know what this means.